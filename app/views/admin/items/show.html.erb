<h1>商品詳細</h1>
<div>
  <div>
    <%= image_tag @item.get_image(300,300) %>
  </div>
  
  <div>
    <p>商品名 <%= @item.name %></p>
    <p>商品説明 <%= @item.introduction %></p>
    <p>税込価格(税抜価格)  <%= @item.add_tax_price.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>)円</p>
  </div>
  
</div>

<%= link_to "編集", admin_items_edit_path, class: "btn btn-success" %>
